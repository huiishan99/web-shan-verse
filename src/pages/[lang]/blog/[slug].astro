---
import type { CollectionEntry } from 'astro:content';
import BlogPostPage from '../../../components/pages/BlogPostPage.astro';
import type { Locale } from '../../../i18n/config';
import { getLocalizedBlogPostStaticPaths } from '../../../utils/blog';

export async function getStaticPaths() {
  return getLocalizedBlogPostStaticPaths();
}

type Props = {
  post: CollectionEntry<'blog'>;
  lang: Locale;
  alternateLocales: Locale[];
};

const { post, lang, alternateLocales } = Astro.props;
---

<BlogPostPage post={post} lang={lang} alternateLocales={alternateLocales} />
