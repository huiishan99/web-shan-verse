---
import type { TrustedInlineHtml } from '../utils/html';

interface Props {
  html: TrustedInlineHtml;
}

const { html } = Astro.props;
---

<!-- Repository-authored localized inline HTML only. Do not pass user input here. -->
<Fragment set:html={html} />
